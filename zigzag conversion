public class Solution {
    public String convert(String s, int numRows) {
        // 1. Handle edge cases where no transformation is needed
        if (numRows == 1 || numRows >= s.length()) {
            return s;
        }

        // 2. Create an array of StringBuilders for each row
        StringBuilder[] rows = new StringBuilder[numRows];
        for (int i = 0; i < numRows; i++) {
            rows[i] = new StringBuilder();
        }

        // 3. Distribute characters into rows
        int currentRow = 0;
        boolean goingDown = false;

        for (char c : s.toCharArray()) {
            rows[currentRow].append(c);

            // Flip direction when hitting the top or bottom boundary
            if (currentRow == 0 || currentRow == numRows - 1) {
                goingDown = !goingDown;
            }

            // Move pointer up or down
            currentRow += goingDown ? 1 : -1;
        }

        // 4. Merge rows into a single string
        StringBuilder result = new StringBuilder();
        for (StringBuilder row : rows) {
            result.append(row);
        }

        return result.toString();
    }
}
